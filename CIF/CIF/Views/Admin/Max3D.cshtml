@model IEnumerable<CIF.Models.ArchModelView>

@{
    ViewBag.Title = "Max3D";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Max3D</h2>

<p>
    @Html.ActionLink("Thêm", "Add3DMax")
</p>
<table class="table">
    <tr>
        <th>
            Hình
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DownloadLink)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AddDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DESC)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            <img src="@Url.Content("~/Content/Image3DMax/" + item.Id + ".png")" width="70" height="70" />
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DownloadLink)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AddDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DESC)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit3DMax", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete3DMax", new { id=item.Id })
        </td>
    </tr>
}

</table>
